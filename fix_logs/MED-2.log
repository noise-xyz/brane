I've completed the fix for **MED-2**. Here's a summary of what was done:

## Summary

**Issue:** Make RpcRetry backoff constants configurable. Extract retry constants to RpcRetryConfig record. Allow custom config injection via builder pattern.

**Changes made:**

1. **Created `RpcRetryConfig.java`** - A new record with:
   - `backoffBaseMs` - base delay in milliseconds (default: 200ms)
   - `backoffMaxMs` - maximum delay cap (default: 5000ms)
   - `jitterMin` - minimum jitter percentage (default: 10%)
   - `jitterMax` - maximum jitter percentage (default: 25%)
   - Builder pattern for easy customization
   - Validation in the canonical constructor
   - `defaults()` factory method

2. **Updated `RpcRetry.java`**:
   - Replaced hardcoded constants with `RpcRetryConfig`
   - Added overloaded `run(supplier, maxAttempts, config)` method
   - Added overloaded `runRpc(supplier, maxAttempts, config)` method
   - Original methods delegate to new methods with default config
   - Updated `backoff()` to accept config parameter

3. **Added tests in `RpcRetryTest.java`**:
   - `runWithCustomConfigUsesConfiguredBackoff` - verifies custom config is applied
   - `runRpcWithCustomConfigUsesConfiguredBackoff` - verifies for runRpc method
   - `configDefaultsMatchExpectedValues` - verifies default values
   - `configBuilderAllowsPartialCustomization` - verifies builder pattern
   - `configValidatesParameters` - verifies parameter validation
