Issue: HIGH-5 - Refactor MulticallBatch to use type-safe call API
File: MulticallBatch.java:336

Status: ALREADY FIXED

Analysis:
The code at line 335 (line number may have shifted since the review) already uses
the type-safe call API:

    final String resultHex = publicClient.call(callRequest, BlockTag.LATEST).value();

This uses:
- CallRequest (type-safe request object) instead of Map<String, Object>
- BlockTag.LATEST (enum) instead of String "latest"
- Returns HexData via .value() instead of nullable String

The deprecated API signature is:
    @Deprecated String call(Map<String, Object> callObject, String blockTag);

The current code correctly uses:
    HexData call(CallRequest request, BlockTag blockTag);

No changes required - the code was already refactored to use the type-safe API.

Verified: Unit tests pass.
