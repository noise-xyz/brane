Done. Here's a summary of the fix for **MED-5**:

## Summary

Added timeout support to `WebSocketProvider.sendAsyncBatch` method:

1. **New overloaded method**: `sendAsyncBatch(String method, List<?> params, Duration timeout)` - allows specifying a custom timeout for batch requests

2. **Default timeout behavior**: The original `sendAsyncBatch(String method, List<?> params)` now delegates to the new overload with `defaultRequestTimeout`, providing consistent timeout behavior matching the `sendAsync` methods

3. **Timeout implementation**:
   - Schedules timeout via Netty event loop (same pattern as `sendAsync`)
   - Properly removes timed-out requests from `pendingRequests` map
   - Reports timeouts to metrics via `onRequestTimeout(method, id)`
   - Throws `RpcException` with error code -32000 on timeout

4. **Updated Javadoc**: Removed outdated note that claimed the method didn't support timeouts; added `@see` reference to the new timeout-enabled overload

**Commit**: `cd664a8` - `feat(rpc): add timeout support to WebSocketProvider.sendAsyncBatch [MED-5]`
